rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // كل معلمة تصل فقط لوثيقتها الخاصة: preps/{uid}
    // لا ترى معلمة تحضير غيرها أبداً
    match /preps/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
